# Classes

A collection of useful classes.

## Dictionary

A dictionary that supports getting, setting and deleting attributes.

```python
>>> d = sfera.Dictionary(x=1)
>>> d
{'x': 1}
>>> d.x
1
>>> d.x = 2
>>> del d.x
```